import db from "../models/index.js";
const Clothingstyle = db.clothingstyle;
const Service = db.services;

export const createClothingstyle = async (req, res) => {
    try {
        
    } catch (error) {
        console.log(error);
    }
}
